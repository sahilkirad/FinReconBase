# Next.js API proxy target for /api/v1/* rewrites (same-origin -> no CORS).
#
# Docker (compose network): leave unset -> http://backend-api:8000
# Local dev on the host:   create .env.local with:
#   API_PROXY_TARGET=http://localhost:8000
